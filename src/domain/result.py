"""Enum for possible FizzBuzz results."""

from enum import Enum


class Result(Enum):
    """Enum representing possible FizzBuzz outputs."""
    FIZZ = "Fizz"
    BUZZ = "Buzz"
    FIZZBUZZ = "FizzBuzz"
